<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pbo.projectnavigasi">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PROJECTNAVIGASI">
        <activity android:name=".SukunPasar"></activity>
        <activity android:name=".SukunRumahSakit" />
        <activity android:name=".SukunKantorPolisi" />
        <activity android:name=".SukunSPBU" />
        <activity android:name=".LowokwaruPasar" />
        <activity android:name=".LowokwaruRumahSakit" />
        <activity android:name=".LowokwaruKantorPolisi" />
        <activity android:name=".LowokwaruSPBU" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".PasarMaps"
            android:label="@string/title_activity_hotel_maps" />
        <activity
            android:name=".RumahSakitMaps"
            android:label="@string/title_activity_rumah_sakit_maps" />
        <activity
            android:name=".KantorPolisiMaps"
            android:label="@string/title_activity_kantor_polisi_maps" />
        <activity
            android:name=".SPBUMaps"
            android:label="@string/title_activity_spbumaps" />
        <activity android:name=".KlojenPasar" />
        <activity android:name=".KlojenRumahSakit" />
        <activity android:name=".KlojenKantorPolisi" />
        <activity android:name=".KlojenSPBU" />
        <activity android:name=".Klojen" />
        <activity android:name=".Lowokwaru" />
        <activity android:name=".Sukun" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>